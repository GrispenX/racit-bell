# Copy to .env, then set this to the same value as app/server-data/config.json's google_client_id.
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# The single public nginx port. Backend endpoints are available under /api/ through it.
PORT=8080
